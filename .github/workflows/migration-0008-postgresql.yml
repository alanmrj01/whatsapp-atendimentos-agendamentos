name: PostgreSQL migration validation

on:
  pull_request:
    paths:
      - 'alembic/**'
      - 'app/api/operational_pwa.py'
      - 'app/auth/**'
      - 'app/models/**'
      - 'app/platform_admin/**'
      - 'tests/integration/test_migration_0008_postgresql.py'
      - 'tests/integration/test_booking_postgresql.py'
      - 'tests/test_booking_availability.py'
      - 'tests/test_conversation_engine.py'
      - 'tests/test_conversation_interpreter.py'
      - 'tests/test_operational_conversations.py'
      - 'tests/test_operational_conversations_migration.py'
      - 'tests/integration/test_operational_pwa_postgresql.py'
      - 'tests/test_operational_migration_0008.py'
      - 'tests/test_operational_pwa.py'
      - 'tests/test_access_history.py'
      - 'tests/test_business_access_migration.py'
      - 'tests/test_migration.py'
      - 'requirements*.txt'
      - '.github/workflows/migration-0008-postgresql.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  migration-validation:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: alovia_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d alovia_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12

    env:
      TEST_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/alovia_test
      ALEMBIC_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/alovia_test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check -r requirements.txt

      - name: Physical migration roundtrip
        run: >-
          pytest
          tests/integration/test_migration_0008_postgresql.py::test_migration_0008_roundtrip_preserves_pending_status_and_notes
          -q -p no:cacheprovider

      - name: Access history and directed operational tests
        run: >-
          pytest
          tests/test_access_history.py
          tests/test_business_access_migration.py
          tests/test_migration.py
          tests/test_operational_migration_0008.py
          tests/test_operational_pwa.py
          tests/test_operational_conversations.py
          tests/test_operational_conversations_migration.py
          tests/test_booking_availability.py
          tests/test_conversation_interpreter.py
          tests/test_conversation_engine.py
          -q -p no:cacheprovider

      - name: Physical booking engine tests
        run: >-
          pytest
          tests/integration/test_booking_postgresql.py
          -q -p no:cacheprovider

      - name: Compile application
        run: python -m compileall app

      - name: Check dependencies
        run: python -m pip check

      - name: Diff check
        run: git diff --check
